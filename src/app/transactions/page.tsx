import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Transactions .:: CFC-Pay',
  description: 'Simple payment app',
};

const Transactions = () => {
  return <div>Transactions</div>;
};

export default Transactions;
